server:
  port: 8081

spring:
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/data_engine?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
    type: com.alibaba.druid.pool.DruidDataSource

    #Spring Boot ???????????????????
    #druid ???????
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true

  # redis??
  data:
    redis:
      host: localhost
      port: 6379


# ??????
logging:
  level:
    root: info


#jwt ?????
Jwt:
  #jwt????
  secretKey: sdsdsd23232323

